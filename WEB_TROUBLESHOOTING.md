# Shinobu Translator Web 故障排查

Shinobu Translator Web 是志愿维护的非盈利开源项目，不提供 SLA、固定响应时间或云端任务恢复。
重要结果请导出 ZIP 或 `.shinobu.zip` 项目包自行备份。

## 先确认支持范围

- 桌面正式目标：Windows、macOS、Linux 上最新两个稳定版 Chrome / Edge；
- Android Chrome：Beta；
- iOS / iPadOS 26+：实验支持；
- Firefox、桌面 Safari、旧 iOS、私密浏览与能力测试失败的环境不开放首发本地推理。

最终以设置页显示的真实能力测试为准，而不是只看浏览器名称。

## 能力测试或模型启动失败

1. 使用 HTTPS 或 `localhost`，关闭会破坏 COOP/COEP 的代理或扩展。
2. 更新浏览器和显卡驱动，重新加载页面；页面从后台恢复时会自动重测并重建推理 Worker。
3. WebGPU 失败时应用会降级到 WASM 并降低工作像素档位，不支持强制绕过。
4. 清理站点模型后重新下载；不要手工替换 OPFS 中的文件。
5. 若设置页显示模型能力失败，只报告错误码和阶段，不要粘贴模型字节或完整控制台日志。

## 模型下载失败、暂停或返回 503

- 页面进入后台会主动暂停下载；回到前台后由用户点击继续。
- 下载支持 Range 断点续传，完成全部大小和 SHA-256 校验后才切换版本。
- 503 可能是项目的停机开关或 Workers 免费额度 fail-closed，正常设计是不绕过网关继续放量。
- 若浏览器可用站点空间不足 600 MiB，或不足双版本模型包加 100 MiB，安装会被阻止。
- 项目不会自动删除历史腾空间；先导出重要结果，再在应用或浏览器设置中手动清理。

## 翻译提供商或 CORS 失败

- Base URL 必须是 HTTPS；只有 `localhost`、`127.0.0.0/8` 和 `::1` 可使用 HTTP。
- 应用不再要求单独运行 CORS 预检；首次真实翻译请求会直接接受浏览器的 CORS 检查。
- 鉴权或 CORS 错误不会自动重试；429、网络错误和 5xx 最多退避重试两次。
- 超时请求可能已被提供商接收，应用不会擅自重发；由用户决定是否重试。
- Base URL 的域名、端口或 API 路径前缀改变后，设备保存的 Key 会失效并要求重新确认。

## 图片导入或项目包失败

- 只支持静态 PNG、JPEG、WebP、AVIF；GIF、动画 WebP/APNG、SVG、TIFF、HEIC/HEIF 会拒绝。
- 桌面单文件上限 32 MiB，移动端 20 MiB；单批最多 100 张、原文件合计 500 MiB。
- 40 MP 以内但超过当前工作档位的图片会创建缩小工作副本，原文件不会被覆盖。
- 项目包必须通过版本、路径、类型、声明大小和 SHA-256 完整校验，失败时不会写入半个项目。
- `.shinobu.zip` 未加密，可能包含原图、结果、OCR 和译文，请自行保护。

## 离线、后台和更新

- 离线只允许打开应用、查看历史和下载已有结果，不启动新任务。
- 页面隐藏后不再领取下一张，并保存恢复点；移动系统仍可能随时终止页面。
- Service Worker 不缓存翻译 API 请求或响应。
- 新版本在批次空闲后激活，避免运行中替换代码。

## 安全地提交问题

设置页的“脱敏诊断”只包含版本、设备能力、处理阶段、稳定错误码、存储量和提供商主机。它不包含：

- 图片、缩略图、文件名或项目包；
- OCR、译文、提示词或请求/响应正文；
- API Key、完整提供商 URL、URL 路径或查询参数；
- 原始错误消息。

导出后仍请先人工打开检查，再使用
[Web 版问题模板](https://github.com/DonutShinobu/ShinobuTranslator/issues/new?template=web-bug.yml)
提交。公开 Issue 中不要上传敏感数据。
