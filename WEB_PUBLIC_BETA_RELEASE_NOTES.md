# Shinobu Translator Web Public Beta 发布说明

> 状态：发布草案。生产配置完成且自动检查通过后即可创建 Public Beta；Android 真机验证安排在发布后，iOS 暂不作为首发条件。

## 本次提供什么

Shinobu Translator Web 将现有浏览器扩展的漫画图片处理流水线带到全屏网页工作台：

- 导入单张或有序批量的静态 PNG、JPEG、WebP、AVIF；
- 运行“翻译”“原文重排”“仅擦除”三种模式；
- 在浏览器本地完成检测、日文 OCR、擦除、修复和排版；
- 串行执行任务，并支持追加、调整或移除尚未开始的图片；
- 保存本地历史、恢复点、结果 PNG/ZIP 和未加密的 `.shinobu.zip` 项目包；
- 作为 PWA 安装，并在离线时查看、导出已有历史；
- 直接连接用户选择并自行配置的文本翻译服务。

Web 版不提供账户、云同步、共享翻译额度、服务端图片处理、遥测或远程错误上报。

## 支持范围

- **桌面正式范围**：Windows、macOS、Linux 上最新两个稳定版 Chrome 或 Edge。最终是否允许推理取决于启动时的真实能力测试，而不是浏览器名称。
- **Android Beta**：最新稳定版 Android Chrome。相机通过系统文件捕获入口打开，不申请持续摄像头权限。
- **iOS/iPadOS 实验范围**：iOS/iPadOS 26 及以上的 Safari 或 Chrome。移动系统可能冻结页面，不能承诺后台持续推理。
- **不作为首发推理平台**：Firefox、桌面 Safari、iOS/iPadOS 25 及以下、私密浏览，以及未通过能力测试的环境。这些环境仍可能查看设置或已有本地结果。

任何设备都不保证获得最高工作分辨率。低内存、WASM 降级或能力测试失败会降低工作像素档位，且不能由用户强制绕过。

## 图片、文本与凭据边界

- 原图与视觉模型推理留在浏览器本地，不发送给 Shinobu Translator、Cloudflare 模型网关或文本翻译提供商。
- “翻译”模式会把 OCR 得到的文字和本页上下文直接发送给用户选择的服务；“原文重排”和“仅擦除”不调用文本翻译服务。
- API Key 默认只保留在当前标签页会话。选择“记住此设备”时，Key 使用不可导出的 WebCrypto 设备密钥加密后写入本地存储。
- 应用不创建账户、用户 ID 或自定义访问日志。Cloudflare 和用户选择的翻译服务仍会处理提供服务所需的网络元数据。
- 用户主动导出的脱敏诊断只包含版本、能力、阶段、稳定错误码、存储量和提供商主机，不包含图片、文件名、OCR、译文、请求正文或 API Key。

完整说明见 [隐私政策](PRIVACY_POLICY.md)。

## 模型与离线行为

首次运行需要下载约 196 MiB 的本地模型包，文件保存在浏览器站点数据中。下载支持校验、取消、重试和断点续传；全部哈希通过后才会原子启用新版本。

模型下载使用私有 Cloudflare R2 和限额内 fail-closed 的 Worker。若免费额度耗尽、人工停机或服务遭滥用，当天可能无法安装模型；项目不会为了继续放量而切换到可产生不可控费用的公开路径。

离线时可以打开应用、查看历史和下载已有结果，但不能启动新的翻译、原文重排或擦除任务。

## 本地存储与项目包

浏览器可能拒绝持久存储或清理站点数据。历史记录只是本地便利缓存，重要结果应导出为结果 ZIP 或项目包。

`.shinobu.zip` 项目包未加密，可能包含原图、结果、缩略图、OCR、译文、配置、顺序和状态；不包含模型、API Key 或诊断日志。请像保护原始漫画文件一样保护项目包。

## 已接受的限制

- 自定义 HTTPS 或本机翻译服务仍受浏览器 CORS 和本地网络权限限制。
- 已送达翻译服务但响应状态未知的请求不会自动重发，由用户决定是否再次提交。
- Workers 免费额度耗尽时，模型下载会停止服务而不是继续计费。
- 移动 OS 可能随时冻结或终止页面，PWA 不具备后台持续推理特权。
- 本项目通过 GitHub Issues、故障排查文档和用户主动导出的脱敏诊断提供社区支持，不承诺 SLA。

使用问题和安全反馈方式见 [Web 故障排查](WEB_TROUBLESHOOTING.md)，第三方组件与模型许可见 [第三方声明](THIRD_PARTY_NOTICES.md)。
